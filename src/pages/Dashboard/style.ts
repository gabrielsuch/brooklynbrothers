import styled from "styled-components"


export const Container = styled.main `
    width: 100%;

    .products {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: center;
    }
`